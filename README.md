# ITEM_BUILDER

Backend API for Item Builder (Express + TypeScript + MongoDB).

## Setup

```bash
npm install
cp .env.example .env   # then edit .env with your values
npm run dev
```

## Docs

- Swagger UI: `http://localhost:8080/docs`
- OpenAPI: `http://localhost:8080/docs.json`
